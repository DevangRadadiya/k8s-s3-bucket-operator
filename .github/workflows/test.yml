name: Test

# Opt into Node 24 for JS-based actions (see GitHub deprecation of Node 20 runners).
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  unit-lint:
    name: Unit + Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Go vet
        run: make lint

      - name: Go test
        run: go test ./... -v -coverprofile=coverage.out

  e2e-kind:
    name: E2E (kind, Kubernetes)
    runs-on: ubuntu-latest
    needs: unit-lint
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      # Default cluster name is chart-testing; `kind load` defaults to cluster "kind" and would miss the image.
      - name: Setup kind
        uses: helm/kind-action@v1.14.0

      - name: Build and load operator image into kind
        run: |
          docker build -t k8s-s3-bucket-operator:test .
          echo "Kind clusters:" && kind get clusters
          for c in $(kind get clusters); do
            echo "Loading image into cluster: $c"
            kind load docker-image k8s-s3-bucket-operator:test --name "$c"
          done

      - name: Run E2E
        env:
          OPERATOR_IMAGE: k8s-s3-bucket-operator:test
        run: ./test/e2e/run.sh k8s

  e2e-openshift-profile:
    name: E2E (kind, OpenShift profile)
    runs-on: ubuntu-latest
    needs: unit-lint
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Setup kind
        uses: helm/kind-action@v1.14.0

      # Run the "OpenShift profile" E2E. The script falls back to plain Kubernetes deploy
      # if OpenShift SCC APIs are not available (so it can still run on kind).
      - name: Build and load operator image into kind
        run: |
          docker build -t k8s-s3-bucket-operator:test .
          echo "Kind clusters:" && kind get clusters
          for c in $(kind get clusters); do
            echo "Loading image into cluster: $c"
            kind load docker-image k8s-s3-bucket-operator:test --name "$c"
          done

      - name: Run OpenShift-profile E2E
        env:
          OPERATOR_IMAGE: k8s-s3-bucket-operator:test
        run: ./test/e2e/run.sh openshift

  e2e-aws-localstack:
    name: E2E (kind, AWS backend + LocalStack)
    runs-on: ubuntu-latest
    needs: unit-lint
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Setup kind
        uses: helm/kind-action@v1.14.0

      - name: Build and load operator image into kind
        run: |
          docker build -t k8s-s3-bucket-operator:test .
          echo "Kind clusters:" && kind get clusters
          for c in $(kind get clusters); do
            echo "Loading image into cluster: $c"
            kind load docker-image k8s-s3-bucket-operator:test --name "$c"
          done

      - name: Run AWS LocalStack E2E
        env:
          OPERATOR_IMAGE: k8s-s3-bucket-operator:test
        run: ./test/e2e/run.sh aws

